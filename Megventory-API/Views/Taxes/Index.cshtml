@model IEnumerable<Megventory_API.Models.Taxes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TaxName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaxDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaxValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TaxName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaxDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaxValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.mvTaxes">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.mvTaxes">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.mvTaxes">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
